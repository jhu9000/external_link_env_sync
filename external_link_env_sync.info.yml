name: External Link Environment Sync
description: 'Alters external link hostnames in render output to have the same environment hostname pattern.'
type: module
core_version_requirement: ^10 || ^11
configure: external_link_env_sync.settings
